{{ form_start(form) }}
    {{ form_widget(form) }}
    <br>
    <div class="col-lg-5" style="margin-left:325px;">
    <button class="btn btn-light btn-block">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
