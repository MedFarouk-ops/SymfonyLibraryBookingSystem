{% extends 'admin-base.html.twig' %}

{% block title %}Edit Auteur{% endblock %}

{% block body %}
 <!-- Start wrapper-->
 <div id="wrapper">
    
    {% include 'admin/sidebar.html.twig' %}
    {% include 'admin/header.html.twig' %}

    <div class="clearfix"></div>	
    <div class="content-wrapper">
    <div class="container-fluid">
    <div class="col-lg-16">
    <div class="card">
    <div class="card-body">
    <div class="card-title">Modifier auteur : </div>

    {{ include('auteur/_form.html.twig', {'button_label': 'Update'}) }}

    <br>
    <div class="col-lg-5" style="margin-left:325px;">
        {{ include('auteur/_delete_form.html.twig') }}
        <br>
        <a href="{{ path('auteur_index') }}" class="btn btn-dark btn-block">Retour</a>
    </div>
    </div>
    </div>
    </div>
    </div>
    </div>
    </div>
{% endblock %}
